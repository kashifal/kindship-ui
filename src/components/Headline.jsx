import React from "react";

const Headline = ({ text }) => {
  return <h1 className="text-black text-left leading-normal tracking-[0.36px] sm:text-center text-[30px] font-bold">{text}</h1>;
};

export default Headline;
